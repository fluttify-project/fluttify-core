@"RefClass::as#__type_name__#": ^(NSObject <FlutterPluginRegistrar> * registrar, NSDictionary<NSString *, id> * args, FlutterResult methodResult) {
    // 引用对象
    NSInteger refId = [args[@"refId"] integerValue];
    id ref = HEAP[@(refId)];

    // 转型
    ref = (#__type_name__# *) ref;
    // 放回HEAP
    HEAP[@(refId)] = ref;

    methodResult(@(refId));
},
